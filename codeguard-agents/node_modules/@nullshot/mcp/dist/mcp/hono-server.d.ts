import { Hono } from 'hono';
import { McpServerDO } from './server';
export declare abstract class McpHonoServerDO<Env extends Record<string, any> = Record<string, any>> extends McpServerDO<Env> {
    private app;
    constructor(ctx: DurableObjectState, env: Env);
    fetch(request: Request): Promise<Response>;
    /**
     * Set up routes for the MCP server
     */
    protected setupRoutes(app: Hono<{
        Bindings: Env;
    }>): void;
}
